apiVersion: v1
kind: Service
metadata:
  name: {{ include "schedule-server.fullname" . }}
  labels:
    {{- include "schedule-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "schedule-server.selectorLabels" . | nindent 4 }}
