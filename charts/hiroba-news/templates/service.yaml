apiVersion: v1
kind: Service
metadata:
  name: {{ include "hiroba-news.fullname" . }}
  labels:
    {{- include "hiroba-news.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hiroba-news.selectorLabels" . | nindent 4 }}
